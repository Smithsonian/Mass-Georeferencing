#Database or files?
backend = "database" #database or files

#Database creds
pg_host = "localhost"
pg_db = ""
pg_user = ""

#Threshold
min_score = 80

#Which collection to run
collex_id = ""

#No. of cores for parallel
no_cores = 8

#Which layers to use
layers = ["gbif.species", "gbif.genus", "wdpa", "gadm", "gnis", "gns", "global_lakes", "geonames"]

#Run spatial match, requires PostGIS
spatial_match = True
